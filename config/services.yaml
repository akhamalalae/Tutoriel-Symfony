parameters:
    directory: '%kernel.project_dir%/public/'
    key_secrets: '%env(KEY_SECRETS)%'
    iv: '%env(IV)%'
    
services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

  

    # Déclaration des handlers
    App\EventListener\Activity\Handler\MessageActivityHandler:
        arguments:
            $activity: '@App\Contracts\Activity\ActivityMessageInterface'

    App\EventListener\Activity\Handler\UserActivityHandler:
        arguments:
            $activity: '@App\Contracts\Activity\ActivityUserInterface'

    App\EventListener\Activity\Handler\FileMessageActivityHandler:
        arguments:
            $activity: '@App\Contracts\Activity\ActivityFileMessageInterface'

    App\EventListener\Activity\Handler\SearchDiscussionActivityHandler:
        arguments:
            $activity: '@App\Contracts\Activity\ActivitySearchDiscussionInterface'

    App\EventListener\Activity\Handler\SearchMessageActivityHandler:
        arguments:
            $activity: '@App\Contracts\Activity\ActivitySearchMessageInterface'

    App\EventListener\Activity\Router\ActivityRouter:
        arguments:
            $handlers:
                - '@App\EventListener\Activity\Handler\MessageActivityHandler'
                - '@App\EventListener\Activity\Handler\UserActivityHandler'
                - '@App\EventListener\Activity\Handler\FileMessageActivityHandler'
                - '@App\EventListener\Activity\Handler\SearchDiscussionActivityHandler'
                - '@App\EventListener\Activity\Handler\SearchMessageActivityHandler'
                # Ajoute ici tous les autres handlers

    App\Services\Message\MessageSearchService:
        tags:
            - controller_service_arguments
        public: true
        arguments:
            - '@fos_elastica.finder.message'

    App\Services\Discussion\DiscussionSearchService:
        tags:
            - controller_service_arguments
        public: true
        arguments:
            - '@fos_elastica.finder.discussion'

    App\Services\File\FileUploader:
        arguments:
            $targetDirectory: '%directory%'

    App\EventListener\DatabaseActivitySubscriber:
        tags:
            - { name: 'doctrine.event_subscriber', connection: 'default' }

    App\EventListener\ExceptionListener:
        tags:
            - { name: kernel.event_listener, event: kernel.exception }
